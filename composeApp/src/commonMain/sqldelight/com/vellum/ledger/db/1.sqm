ALTER TABLE transactions ADD COLUMN original_amount INTEGER NOT NULL DEFAULT 0;
ALTER TABLE transactions ADD COLUMN original_currency TEXT NOT NULL DEFAULT 'LEGACY';
ALTER TABLE transactions ADD COLUMN updated_at INTEGER NOT NULL DEFAULT 0;

-- Update existing transactions to have their current amount as original
UPDATE transactions SET original_amount = amount;
UPDATE transactions SET updated_at = created_at;
